[build]
  functions = "netlify/functions"
  # If your Lucky Daily site is static HTML/CSS/JS, point this at that folder.
  # e.g. publish = "public"
  publish = "public"

[functions]
  node_bundler = "esbuild"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "PADDLE_ENV,PADDLE_PRICE_ID"
